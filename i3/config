# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#
# i3 config file (v4)
# Please see https://i3wm.org/docs/userguide.html for a complete reference!
set $mod Mod4
# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
font pango:Fira Code 10

# This font is widely installed, provides lots of unicode glyphs, right-to-left
# text rendering and scalability on retina/hidpi displays (thanks to pango).
#font pango:DejaVu Sans Mono 8

# The combination of xss-lock, nm-applet and pactl is a popular choice, so
# they are included here as an example. Modify as you see fit.

# xss-lock grabs a logind suspend inhibit lock and will use i3lock to lock the
# screen before suspend. Use loginctl lock-session to lock your screen.
exec --no-startup-id xss-lock --transfer-sleep-lock -- i3lock --nofork

# NetworkManager is the most popular way to manage wireless networks on Linux, and nm-applet is a desktop environment-independent system tray GUI for it. exec --no-startup-id nm-applet
# Use pactl to adjust volume in PulseAudio.
set $refresh_i3status killall -SIGUSR1 i3status

# ================= Map volume ========================
# bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute @DEFAULT_SINK@ toggle && $refresh_i3status
# bindsym XF86AudioMicMute exec --no-startup-id pactl set-source-mute @DEFAULT_SOURCE@ toggle && $refresh_i3status
# bindsym $mod+plus exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ +10% && $refresh_i3status
# bindsym $mod+minus exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ -10% && $refresh_i3status
set $changevolumeup exec ~/.config/dunst/./changevolume.sh up
set $changevolumedown exec ~/.config/dunst/./changevolume.sh down
set $changevolumemute exec ~/.config/dunst/./changevolume.sh mute
bindsym XF86AudioRaiseVolume $changevolumeup
bindsym XF86AudioLowerVolume $changevolumedown
bindsym XF86AudioMute $changevolumemute

bindcode $mod+96 $changevolumeup
bindcode $mod+95 $changevolumedown
bindcode $mod+76 $changevolumemute

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# start a terminal
bindsym $mod+Return exec i3-sensible-terminal

# kill focused window
bindsym $mod+q kill; exec $changeWorkspace

# start dmenu Rofi (a program launcher)
# bindsym $mod+d exec rofi -show drun 
bindsym $mod+d exec $HOME/.config/rofi/launchers/misc/launcher.sh
bindsym $mod+Shift+d exec $HOME/.config/rofi/rofi-wifi-menu.sh

# change focus
bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right
# bindsym $mod+semicolon focus right
# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
bindsym $mod+Shift+h move left
bindsym $mod+Shift+j move down
bindsym $mod+Shift+k move up
bindsym $mod+Shift+l move right

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
bindsym $mod+Shift+v split h

# split in vertical orientation
bindsym $mod+v split v

# enter fullscreen mode for the focused container
bindsym $mod+space fullscreen toggle; exec $HOME/.config/polybar/setup.sh 

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+f floating toggle

# change focus between tiling / floating windows
bindsym $mod+f focus mode_toggle

# focus the parent container
bindsym $mod+a focus parent

# focus the child container
#bindsym $mod+d focus child

# Define names for default workspaces for which we configure key bindings later on.
# We use variables to avoid repeating the names in multiple places.
set $ws1 "1"
set $ws2 "2"
set $ws3 "3"
set $ws4 "4"
set $ws5 "5"
set $ws6 "6"
set $ws7 "7"
set $ws8 "8"
set $ws9 "9"
set $ws10 "10"
set $changeWorkspace "$HOME/.config/polybar/setup.sh"

# switch to workspace
set $randompicture exec feh --bg-scale --randomize ~/.config/Arch\ Config/Anime/*

bindsym $mod+1 workspace number $ws1; exec $changeWorkspace; $randompicture
bindsym $mod+2 workspace number $ws2; exec $changeWorkspace; $randompicture
bindsym $mod+3 workspace number $ws3; exec $changeWorkspace; $randompicture
bindsym $mod+4 workspace number $ws4; exec $changeWorkspace; $randompicture
bindsym $mod+5 workspace number $ws5; exec $changeWorkspace; $randompicture
bindsym $mod+6 workspace number $ws6; exec $changeWorkspace; $randompicture
bindsym $mod+7 workspace number $ws7; exec $changeWorkspace; $randompicture
bindsym $mod+8 workspace number $ws8; exec $changeWorkspace; $randompicture
bindsym $mod+9 workspace number $ws9; exec $changeWorkspace; $randompicture
bindsym $mod+0 workspace number $ws10; exec $changeWorkspace; $randompicture

assign [class="Brave-browser"] $ws3
assign [class="Org.gnome.Nautilus"] $ws4
assign [class="MongoDB Compass"] $ws9

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace number $ws1; exec $changeWorkspace
bindsym $mod+Shift+2 move container to workspace number $ws2; exec $changeWorkspace
bindsym $mod+Shift+3 move container to workspace number $ws3; exec $changeWorkspace
bindsym $mod+Shift+4 move container to workspace number $ws4; exec $changeWorkspace
bindsym $mod+Shift+5 move container to workspace number $ws5; exec $changeWorkspace
bindsym $mod+Shift+6 move container to workspace number $ws6; exec $changeWorkspace
bindsym $mod+Shift+7 move container to workspace number $ws7; exec $changeWorkspace
bindsym $mod+Shift+8 move container to workspace number $ws8; exec $changeWorkspace
bindsym $mod+Shift+9 move container to workspace number $ws9; exec $changeWorkspace
bindsym $mod+Shift+0 move container to workspace number $ws10; exec $changeWorkspace

# reload the configuration file
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r exec pkill picom; exec picom; restart
# exit i3 (logs you out of your X session)
# bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -B 'Yes, exit i3' 'i3-msg exit'"

# ===================== Gaps =============================
# You can also use any non-zero value if you'd like to have a border
for_window [class=".*"] border pixel 4

set $visible true
# Examples
gaps inner 7
# gaps outer 5
gaps top 40

# ===================== Resize ================================
# resize window (you can also use the mouse for that)
mode "resize" {
  # These bindings trigger as soon as you enter the resize mode

  # Pressing left will shrink the window’s width.
  # Pressing right will grow the window’s width.
  # Pressing up will shrink the window’s height.
  # Pressing down will grow the window’s height.
  bindsym h resize shrink width 1 px or 1 ppt
  bindsym j resize grow height 1 px or 1 ppt
  bindsym k resize shrink height 1 px or 1 ppt
  # bindsym semicolon resize grow width 1 px or 1 ppt
  bindsym l resize grow width 1 px or 1 ppt

  # same bindings, but for the arrow keys
  bindsym Left resize shrink width 10 px or 10 ppt
  bindsym Down resize grow height 10 px or 10 ppt
  bindsym Up resize shrink height 10 px or 10 ppt
  bindsym Right resize grow width 10 px or 10 ppt

  # back to normal: Enter or Escape or $mod+r
  bindsym Return mode "default"
  bindsym Escape mode "default"
  bindsym $mod+r mode "default"
}

bindsym $mod+r mode "resize"

# ==================== Wallpaper =======================
exec_always feh --bg-scale --randomize ~/.config/Arch\ Config/Spaces/*
# ===================== Color ============================
# color defines for zenburn styled i3
set $bg #2c2c2e
set $fg #9f9f9f
set $hi #efef8f
set $ac #61AFEF
set $tx #040404
set $ia #8f8f8f
set $be #8faf9f
set $yw #ccdc90
set $gn #88b090
set $rd #e89393

# set some nice colors      border     background  text   indicator child_border
client.focused              $ac        $ac         $tx     $ac      $ac
client.unfocused            $bg        $bg         $ia
client.focused_inactive     $bg        $bg         $ac
client.urgent               $rd        $rd         $tx

# ========================= Light ==========================
set $changebrightup exec ~/.config/dunst/./changebright.sh up
set $changebrightdown exec ~/.config/dunst/./changebright.sh down
# bindsym XF86MonBrightnessUp exec light -A 1 # increase screen brightness
# bindsym XF86MonBrightnessDown exec light -U 1 # decrease screen brightness
# bindsym $mod+Shift+plus exec light -A 1 # increase screen brightness
# bindsym $mod+Shift+minus exec light -U 1 # decrease screen brightness

bindsym XF86MonBrightnessUp $changebrightup # increase screen brightness
bindsym XF86MonBrightnessDown $changebrightdown # decrease screen brightness
bindsym $mod+Shift+plus $changebrightup # increase screen brightness
bindsym $mod+Shift+minus $changebrightdown # decrease screen brightness

# ================= Moving workspace =================
# bindsym $mod+Tab workspace next
bindsym $mod+Tab workspace back_and_forth;exec $changeWorkspace
bindsym $mod+Shift+Tab workspace prev;exec $changeWorkspace

# =================== Ibus Vn-Eng =======================

bindcode $mod+34 exec $HOME/.config/i3/changelanguage/run.sh
# exec_always ibus-daemon
# bindcode $mod+35 exec ibus engine Bamboo >/dev/null 2>&1 
# bindcode $mod+34 exec ibus engine xkb:us::eng >/dev/null 2>&1 

# ===================== FlameShot =========================
bindsym $mod+Shift+s exec flameshot gui 

# ====================== CapsLock =========================
bindcode $mod+66 exec $HOME/.config/i3/togglecapslock/run.sh

# /usr/share/doc/picom/examples
exec_always picom
# exec_always --no-startup-id compton 
# --config ~/.config/i3/compton.conf

# ======================= Config Ranger =================
# exec_always export LC_ALL=en_US.UTF-8

# ========================= Config colorls ==================
# exec_always export PATH="/home/caparies/.local/share/gem/ruby/3.0.0/bin:$PATH"

# =========================== Polybar =========================
exec_always --no-startup-id $HOME/.config/polybar/launch.sh

# =========================== Toggle powermenu =========================
bindcode $mod+33 exec $HOME/.config/rofi/powermenu/powermenu.sh

# =========================== Toggle polybar =========================
bindsym $mod+b exec $HOME/.config/i3/togglepolybar/run.sh

# =========================== Toggle greenclip =========================
# greenclip clear
# bindcode $mod+54 exec greenclip daemon
# bindsym $mod+Shift+c exec rofi -modi "clipboard:greenclip print" -show clipboard -run-command '{cmd}' && pkill greenclip

# =========================== Change Theme =========================
bindcode $mod+51 exec $HOME/.config/polybar/scripts/switchcolors/color_switch.sh

# =========================== Copy Text =========================
bindsym $mod+c exec copyq menu

# =========================== Conky =========================
exec_always $HOME/.conky/conky-startup.sh

# =========================== Pass =====================
# caotuankietc3a
# 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
