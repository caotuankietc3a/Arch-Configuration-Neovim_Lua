#!/bin/bash

SDIR="$HOME/.config/polybar/scripts"

# replacing theme scripts in polybar
function scripts_polybar_changes() {
  if [ $1 == "tokyo-dark" ]
  then
  elif [ $1 == "dracula" ]
  then
  elif [ $1 == "tokyo-night" ]
  then
  fi
}
